kind = "web"
previewPath = "/"
title = "To Tell The Truth Dashboard"
version = "1.0.0"
id = "artifacts/tttt-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24809

[services.development]
run = "pnpm --filter @workspace/tttt-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tttt-dashboard", "run", "build" ]
publicDir = "artifacts/tttt-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24809"
BASE_PATH = "/"
